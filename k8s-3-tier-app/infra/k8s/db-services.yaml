apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: default
spec:
  type: ExternalName
  externalName: k8s-3tier-postgres.cvik8accw2tk.ap-south-1.rds.amazonaws.com
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
