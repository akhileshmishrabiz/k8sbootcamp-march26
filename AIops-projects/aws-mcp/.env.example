# Copy to .env and adjust (optional — setup.sh reads these when present)
AWS_PROFILE=default
AWS_REGION=us-east-1

# Safety: set to true to block write/delete AWS operations via MCP
READ_OPERATIONS_ONLY=false

# Require explicit consent before mutating AWS resources (needs client elicitation support)
REQUIRE_MUTATION_CONSENT=false
