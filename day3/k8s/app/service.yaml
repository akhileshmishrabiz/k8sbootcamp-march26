apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: default
spec:
  selector:
    app: studentportal
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP